# ── Build-time variables (NEXT_PUBLIC_* are inlined during `next build`) ────
# Runtime secrets (WORKOS_API_KEY, DATABASE_URL, etc.) are injected via
# ECS Secrets Manager — they are NOT listed here.

NEXT_PUBLIC_WORKOS_REDIRECT_URI=https://medgraph.vulniq.org/auth/callback

